# Ablation configurations

The paper evaluates modality contribution, sequence-fusion choice, pocket encoder,
SMILES encoder, and fusion strategy. Public YAML files will be added here after the
release audit confirms the exact checkpoint and feature manifest for every row in
Fig. 5. The audited numerical summary is already available in
`results/ablation_summary.csv`.

